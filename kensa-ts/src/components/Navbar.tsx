import * as React from "react";

const Navbar = () => {

    return (
        <div id="navbar">
            <a href="/signup">Sign up</a>
            <a href="/login">Login</a>
        </div>
    );
};

export default Navbar;