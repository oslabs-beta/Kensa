import { Box } from '@chakra-ui/react';
import React from 'react';

const ChartMetrics = () => {
  return (
    <Box flex='1' bgColor='lightcyan'>
      Chart Metrics
    </Box>
  );
};

export default ChartMetrics;